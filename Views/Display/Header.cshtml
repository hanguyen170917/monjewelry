@using System.Web
@using DA_Web.Models
@model IEnumerable<DA_Web.ViewModel.TrangsucViewModel>

<link href="/Content/jquery-ui.css" rel="stylesheet" />
<style>
    .ui-menu {
        background: white;
        height: auto;
        max-height: 200px;
        overflow: hidden auto;
    }
</style>
<div class="top">
    <div class="header-top" style="font-size: 17px; background: #000; ">
        <div class="container">
            <div class="row">
                <!-- HEADER-LEFT-MENU START -->
                <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12">
          
                </div>
                <!-- HEADER-LEFT-MENU END -->
                <!-- HEADER-RIGHT-MENU START -->
                <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12">
                    <div class="header-right-menu">
                        @Html.Action("ttDangNhappart", "NguoiDung")
                       
                    </div>
                </div>
                <!-- HEADER-RIGHT-MENU END -->
            </div>
        </div>
    </div>
    <!-- HEADER-MIDDLE START -->
    <section class="header-middle">
        <div class="container">
            <div class="row">
                <div class="col-sm-12">
                    <!-- LOGO START -->
                    <div class="logo">
                        <a href="index.html"><img src="~/Content/img/logo2.png" alt="monjewelry logo" /></a>
                    </div>
                    <!-- LOGO END -->
                    <!-- HEADER-RIGHT-CALLUS START -->
                    <div class="header-right-callus">
                        <h3>Liên hệ cho chúng tôi</h3>
                        <span>0909-456-654</span>
                    </div>
                    <!-- HEADER-RIGHT-CALLUS END -->
                    <!-- CATEGORYS-PRODUCT-SEARCH START -->
                    <div class="categorys-product-search">
                        <form action="/Display/TimKiem" method="get" class="search-form-cat">
                            <div class="search-product form-group">
                                @*<select name="catsearch" class="cat-search">
                                    <option value="">Thương hiệu</option>


                                    @{

                                        List<string> x = new List<string>();

                                    }
                                    @foreach (var item in Model)
                                    {
                                        x.Add(@item.TENLOAI);
                                    }
                                    @{
                                        List<string> distinct = x.Distinct().ToList();
                                        foreach (var value in distinct)
                                        {
                                            <option value="">@value</option>
                                        }
                                    }

                                    }

                                </select>*@
                              

                                
                                <input type="text" id="txtKeyword" class="form-control search-form" name="keyword" placeholder="Nhập sản phẩm tại đây ... " />                            
                                <button class="search-button" value="Search" name="s" type="submit">
                                <i class="fa fa-search"></i>
                                </button>
                               
                              
                            </div>
                        </form>
                    </div>
                    <!-- CATEGORYS-PRODUCT-SEARCH END -->
                </div>
            </div>
        </div>
    </section>
    <!-- HEADER-MIDDLE END -->
    <!-- MAIN-MENU-AREA START -->
    <header class="main-menu-area">
        <div class="container">
            <div class="row">
                <!-- SHOPPING-CART START -->
                <div class="col-lg-3 col-md-3 col-sm-12 col-xs-12 pull-right shopingcartarea">
                    <div class="shopping-cart-out pull-right">
                        <div class="shopping-cart">
                            @Html.Action("ttGioHangpart", "GioHang")
                          
                        </div>
                    </div>
                </div>
                <!-- SHOPPING-CART END -->
                <!-- MAINMENU START -->
                <div class="col-lg-9 col-md-9 col-sm-12 col-xs-12 no-padding-right menuarea">
                    <div class="mainmenu">
                        <nav>
                            <ul class="list-inline mega-menu">
                                <li class="active">
                                    <a href="@Url.Action("TrangChu","Display")">Trang Chủ</a>
                                    <!-- DROPDOWN MENU START -->
                                    <!-- DROPDOWN MENU END -->
                                </li>
                                <li>
                                    <a href="@Url.Action("Hunter", "ThuongHieu", new {id = "all" })">Sản phẩm</a>
                                    <!-- DRODOWN-MEGA-MENU START -->
                                    <div class="drodown-mega-menu">
                                        <div class="left-mega col-xs-6">
                                            <div class="mega-menu-list">
                                                <ul>
                                                    @{
                                                        List<string> n = new List<string>();
                                                        foreach (var it in Model.Where(m => m.ID_LOAI % 2 != 0))
                                                        {

                                                            n.Add(it.TENLOAI);

                                                        }
                                                        List<string> distinct2 = n.Distinct().ToList();
                                                        foreach (var item in distinct2)
                                                        {

                                                            <li><a href="@Url.Action("Hunter", "ThuongHieu", new {id = @item })">@item</a></li>


                                                        }
                                                    }



                                                </ul>
                                            </div>
                                        </div>
                                        <div class="right-mega col-xs-6">
                                            <div class="mega-menu-list">

                                                <ul>
                                                    @{
                                                        List<string> p = new List<string>();
                                                        foreach (var it in Model.Where(m => m.ID_LOAI % 2 == 0))
                                                        {

                                                            p.Add(it.TENLOAI);

                                                        }
                                                        List<string> distinct3 = p.Distinct().ToList();
                                                        foreach (var item in distinct3)
                                                        {

                                                            <li><a href="@Url.Action("Hunter", "ThuongHieu", new {id = @item })">@item</a></li>


                                                        }
                                                    }

                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- DRODOWN-MEGA-MENU END -->
                                </li>

                                <li>
                                    
                                   
                                    <!-- DROPDOWN MENU START -->
                                   
                                    <!-- DROPDOWN MENU END -->
                                </li>
                               
                               
                                <li >
                                    <a href="@Url.Action("Gioithieu","Display")">Giới thiệu</a>
                                </li>
                                @*<li><a href="about-us.html">About us</a></li>*@
                            </ul>
                        </nav>
                    </div>
                </div>
                <!-- MAINMENU END -->
            </div>
            <div class="row">
                <!-- MOBILE MENU START -->
                <div class="col-sm-12 mobile-menu-area">
                    <div class="mobile-menu hidden-md hidden-lg" id="mob-menu">
                        <span class="mobile-menu-title">MENU</span>
                        <nav>
                            <ul>
                                <li>
                                    <a href=@Url.Action("TrangChu","Display")>Trang Chủ</a>
                                </li>
                                <li>
                                    <a href="@Url.Action("Hunter", "ThuongHieu", new {id = "all" })">Sản phẩm</a>
                                    <ul>
                                        <li>
                                            <a href="shop-gird.html">Dây chuyền</a>
                                            <a href="shop-gird.html">Nhẫn nữ</a>
                                            <a href="shop-gird.html">Nhẫn cưới</a>
                                            <a href="shop-gird.html">Bông tai</a>
                                            <a href="shop-gird.html">Charm</a>
                                            <a href="shop-gird.html">Lắc</a>
                                            <a href="shop-gird.html">Kiềng</a>
                                            <a href="shop-gird.html">Nhẫn nam</a>
                                        </li>
                                    </ul>
                                </li>
                                <li>
                                    <a href="@Url.Action("Gioithieu","Display")">Giới thiệu</a>
                                </li>
                            </ul>
                        </nav>
                    </div>
                </div>
                <!-- MOBILE MENU END -->
            </div>
        </div>
    </header>
</div>

